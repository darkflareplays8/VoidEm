{
  "name": "voidem-workspace",
  "version": "2.0.2",
  "private": true,
  "scripts": {
    "build:installer": "cd installer && npm run build",
    "build:emulator": "cd emulator && npm run build",
    "build": "npm run build:installer && npm run build:emulator"
  }
}